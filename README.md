```shell
conda env list
conda activate conda-machine-learning-env
conda create -n anaconda-r-python-env python r-base anaconda
conda activate anaconda-r-python-env
conda deactivate
R --version
python --version
```